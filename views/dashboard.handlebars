{{!-- Dashboard view - new posts + see all posts + make them clickable  --}}

<section>
  <h2>Enter a New Job Application</h2>
{{!-- Parsley Goes Here --}}
  </form>
</section>

{{#if apps.length}}
<section>
  <h2>Your Applications</h2>
  <ol>
    {{#each apps as |apps|}}
    <li>
      {{> apps-info apps}}
      <a href="/dashboard/edit/{{application.id}}" class="edit-link">Edit post</a>
    </li>
    {{/each}}
  </ol>
</section>
{{/if}}

<script src="/javascript/add-post.js"></script>